import React from 'react';

import './index.css';

const Loading = () => {
    return (
        <div className="spinner">
            <div className="spinner-icon"></div>
        </div>
    );
};

export default Loading;
