import React from 'react';

const Loading = () => {
    return (
        <div className="spinner">
            <div className="spinner__object"></div>
        </div>
    );
};

export default Loading;
